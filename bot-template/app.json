{
  "name": "DarkX Mini",
  "description": "A simple WhatsApp bot — .ping, .play, .vv2 — generated by the DarkX Bot Builder",
  "logo": "https://files.catbox.moe/pc5uec.png",
  "keywords": ["bot", "whatsapp", "baileys", "darkx-mini", "mongodb"],
  "env": {
    "MONGODB_URI": {
      "description": "Your own MongoDB connection string, used to store WhatsApp session credentials",
      "required": true
    },
    "BOT_NAME": { "description": "Your bot's display name", "required": false },
    "OWNER_NAME": { "description": "Your name (shown as bot owner)", "required": false },
    "OWNER_NUMBER": { "description": "Your WhatsApp number, digits only with country code", "required": false },
    "PREFIX": { "description": "Command prefix", "value": ".", "required": false },
    "MAX_SESSIONS": { "description": "How many WhatsApp numbers this bot may link at once", "value": "1", "required": false },
    "PORT": { "description": "Port the web dashboard listens on", "value": "3000", "required": false }
  },
  "formation": {
    "web": {
      "quantity": 1,
      "size": "basic"
    }
  }
}
